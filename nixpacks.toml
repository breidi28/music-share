[phases.build]
cmds = ["echo 'Build complete'"]

[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && gunicorn app:app --bind 0.0.0.0:${PORT:-8080} --workers 1 --threads 2 --timeout 120"
